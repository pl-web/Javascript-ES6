// () => { ... }
function foo(x, y) {
  return x + y
}
let add = function (x, y) {
  return x + y
}
// let sum = (x, y) => {
//   return x + y
// }
let sum = (x, y) => x + y
console.log(sum(1, 2))

// () => {}
